[package]
name = "finrust"
version = "0.1.0"
edition = "2024"

[workspace]
members = [
    "workspace/model",
    "workspace/migration",
    "workspace/compute",
]


[dependencies]
model = { path = "workspace/model" }
compute = { path = "workspace/compute" }
